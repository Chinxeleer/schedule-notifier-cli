[package]
name = "database"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"

sea-orm = { version = "1.0.0-rc.5", features = [
  "sqlx-postgres",
  "runtime-tokio-rustls",
  "macros",
  "with-chrono",
  "with-json",
] }
